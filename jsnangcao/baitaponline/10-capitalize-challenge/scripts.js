const myString = 'developer';

myNewString = myString[0].toUpperCase() + myString.substring(1); // lấy ký tự đầu tạo thành chữ hoa + các ký tự sau của chuỗi để tạo thành chuỗi mới

console.log(myNewString); // 'Developer'